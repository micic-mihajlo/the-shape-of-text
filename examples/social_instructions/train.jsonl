{"prompt":"Write a short LinkedIn post for builders about launching a tiny smoke test before a GPU training run.","completion":"Before spending real GPU time, run the smallest version of the system. One tiny smoke test can catch data shape issues, trainer API drift, and save-path mistakes before they become expensive."}
{"prompt":"Write a concise product update about adding prompt-to-post training examples to a social writing model.","completion":"New in the training loop: prompt-to-post examples. The model now sees a real brief, keeps that context, and learns only from the completed post. Small change, much closer to the workflow people actually need."}
{"prompt":"Write a field report for AI builders about validating style metrics after generation.","completion":"Field report: generation is only half the check. We now run held-out briefs through the adapter, write the completions to JSONL, and compare length, density, repetition, and call-to-action markers against the target set."}
